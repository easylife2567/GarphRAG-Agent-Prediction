* [x] `frontend/src/assets/css/main.css` 包含所有必需的暗黑主题 CSS 变量并被全局引入，没有样式冲突。

* [x] `layouts/default.vue` 已更新，所有子路由页面均默认带有统一的暗黑背景、极简网格、发光光晕及毛玻璃导航栏。

* [x] 核心流程页面 (`process`, `simulation`, `report` 等) 的 UI 已全面更新为暗黑极简风格 (Linear/Vercel Aesthetic)。

* [x] D3 图谱可视化组件在暗色背景下显示清晰（发光节点，暗色连线），没有亮色突兀的元素。

* [x] 报告与互动页面的文本在纯黑背景下对比度良好，排版整洁。

* [x] 重构过程严格遵循“仅改 UI，不改逻辑”的约束，没有任何 JS 报错，所有表单提交、拖拽上传、状态流转等原业务逻辑均能正常工作。

