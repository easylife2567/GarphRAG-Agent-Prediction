{
    "name": "garphrag-agent-frontend",
    "private": true,
    "version": "0.1.0",
    "type": "module",
    "scripts": {
        "predev": "rm -rf .nuxt .output",
        "dev": "nuxt dev --host",
        "prebuild": "rm -rf .nuxt .output",
        "build": "nuxt build",
        "preview": "nuxt preview",
        "generate": "nuxt generate",
        "postinstall": "nuxt prepare"
    },
    "dependencies": {
        "d3": "^7.9.0",
        "nuxt": "^3.15.0",
        "pinia": "^2.3.0"
    },
    "devDependencies": {
        "@pinia/nuxt": "^0.9.0",
        "@vue/test-utils": "^2.4.6",
        "jsdom": "^29.0.1",
        "vitest": "^4.1.1"
    }
}
